const LABELS = { youth:"青年与希望", writing:"读书与写作", society:"社会批判", life:"生命与选择", children:"教育与儿童", memory:"纪念与友谊" };
const state = { data:null, filter:"all", query:"" };
function el(tag,className,text){const n=document.createElement(tag);if(className)n.className=className;if(text!==undefined)n.textContent=text;return n;}
function filtered(){const q=state.query.trim().toLowerCase();return state.data.quotes.filter((item)=>(state.filter==="all"||item.category===state.filter)&&(!q||[item.text,item.sourceTitle,item.collection,...item.keywords].join(" ").toLowerCase().includes(q)));}
function showToast(message){const toast=document.querySelector("[data-copy-toast]");toast.textContent=message;toast.hidden=false;clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>{toast.hidden=true;},1800);}
async function copyQuote(item){const value=`“${item.text}”——鲁迅${item.sourceTitle}（${item.year}）`;try{await navigator.clipboard.writeText(value);showToast("原文与出处已复制");}catch{showToast("复制失败，请手动选择原文");}}
function render(){const root=document.querySelector("[data-quote-grid]");const items=filtered();root.replaceChildren();items.forEach((item,index)=>{const card=el("article",`quote-card quote-card--${item.category}`);card.id=`quote-${item.id}`;card.dataset.quoteId=item.id;const head=el("div","quote-card-head");head.append(el("span","quote-number",String(index+1).padStart(2,"0")),el("span","quote-category",LABELS[item.category]));const quote=el("blockquote","quote-text",`“${item.text}”`);const source=el("div","quote-source");source.append(el("strong","",item.sourceTitle),el("span","",`${item.collection} · ${item.year}`));const details=el("details","quote-context");const summary=el("summary","","展开语境");const body=el("div","quote-context-body");body.append(el("p","",item.context));const tags=el("div","quote-tags");item.keywords.forEach((tag)=>tags.append(el("span","",tag)));body.append(tags);details.append(summary,body);const actions=el("div","quote-actions");const copy=el("button","quote-copy","复制原文");copy.type="button";copy.addEventListener("click",()=>copyQuote(item));const link=el("a","quote-origin","原文出处 ↗");link.href=item.sourceUrl;if(/^https:/.test(item.sourceUrl)){link.target="_blank";link.rel="noopener noreferrer";}actions.append(copy,link);card.append(head,quote,source,details,actions);root.append(card);});document.querySelector("[data-quote-count]").textContent=`${items.length} 条语录`;document.querySelector("[data-quote-empty]").hidden=items.length!==0;}
function setFilter(filter){state.filter=filter;document.querySelectorAll("[data-quote-filter]").forEach((button)=>{const active=button.dataset.quoteFilter===filter;button.classList.toggle("is-active",active);button.setAttribute("aria-pressed",String(active));});render();}
function randomQuote(){state.query="";document.querySelector("[data-quote-search]").value="";setFilter("all");const item=state.data.quotes[Math.floor(Math.random()*state.data.quotes.length)];const card=document.querySelector(`#quote-${CSS.escape(item.id)}`);card.classList.add("is-highlighted");card.querySelector("details").open=true;card.scrollIntoView({behavior:"smooth",block:"center"});setTimeout(()=>card.classList.remove("is-highlighted"),2200);}
function renderReferences(){const root=document.querySelector("[data-quote-references]");state.data.references.forEach((item,index)=>{const li=el("li","quote-reference-item");li.append(el("span","",`[${index+1}]`));const copy=el("div");const link=el("a","",item.title);link.href=item.url;link.target="_blank";link.rel="noopener noreferrer";copy.append(link,el("small","",item.publisher));li.append(copy);root.append(li);});}
async function init(){try{const response=await fetch("assets/data/quotes.json");if(!response.ok)throw new Error(`HTTP ${response.status}`);state.data=await response.json();renderReferences();render();document.querySelectorAll("[data-quote-filter]").forEach((button)=>button.addEventListener("click",()=>setFilter(button.dataset.quoteFilter)));document.querySelector("[data-quote-search]").addEventListener("input",(event)=>{state.query=event.target.value;render();});document.querySelector("[data-random-quote]").addEventListener("click",randomQuote);}catch(error){document.querySelector("[data-quote-grid]").replaceChildren(el("p","quotes-error","语录资料暂时无法载入，请刷新页面后重试。"));console.error("Failed to load quote data:",error);}}
init();
